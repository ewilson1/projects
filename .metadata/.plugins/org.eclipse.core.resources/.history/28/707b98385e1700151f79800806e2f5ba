Ti.UI.setBackgroundColor("fff");

//set the Navigation Window from the start, so you can click back to the first page (the open page)
var navWindow = Ti.UI.createNavigationWindow({
	title: "Weather From Around The World",
	backgroundImage: "Weather-Background.jpg",
	top: 20
});

//declare a variable to create the view, which will hold the text
var openView = Ti.UI.createView({
	backgroundColor: "blue",
	border: 1,
	borderRadius: 20,
	left: 50,
	right: 50,
	height: 150,
	width: "100%"
});

//declare a variable to create a label, the text inside the window above
var openText = Ti.UI.createLabel({
	text: "Click here to see the weather pictures",
	color: "white",
	textAlign: "center",
	font: {fontSize: 20, fontfamily: "Arial"}
});

//now add the text (label) to the view
openView.add(open.Text);

//declare a variable to call a second .js page

//add and open the navigation window
navWindow.add(openView);
