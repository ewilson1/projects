Ti.UI.setBackgroundColor("fff");

//create a window to hold the Navigation window
var mainWindow = Ti.UI.createWindow({
	title: "Eric Wilson's Weather",
	backgroundImage: "Weather-Background.jpg"	
});
	var navWindow = Ti.UI.iOS.createNavigationWindow({
		window: mainWindow
	});
		
		var view1 = Ti.UI.createView({
		  backgroundColor: 'white',
		  text: "Click to open up the Menu",
		  top: mainWindow.top +mainWindow.height + 20,
		  //height: 100,
		  width: 300,
		  textAlign: "center",
		  borderRadius: 20
		});
		
		var basicSwitch = Ti.UI.createSwitch({
		  value:true,// mandatory property for iOS 
		  top: view1.top + 20 
		});
		
			basicSwitch.addEventListener('change',function(e){
			  Ti.API.info('Switch value: ' + basicSwitch.value);
			});


// print initial value
Ti.API.info('Switch value: ' + basicSwitch.value);

//add the label, open the main window
navWindow.add(view1, basicSwitch);
navWindow.open();
